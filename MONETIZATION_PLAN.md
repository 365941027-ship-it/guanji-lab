# 观己实验室 · 网页端变现改造方案

> 版本：v1 · 2026-08-22
> 范围：结果页「免费概览 / 分享解锁 / 付费深度解读」三层结构 + 支付接入

## 一、现状盘点（已具备的能力）

| 能力 | 现状 | 文件 |
| --- | --- | --- |
| 深度解读通道 | 已部署 Vercel，DeepSeek 主通道 | `api/interpret.js` |
| AI 深度解读 | 结果页自动生成 3000 字解读，当前**完全免费** | `assets/js/quiz.js:1215` |
| 分享卡片 | 已有保存卡片 / 保存完整结果 | `assets/js/quiz.js:1096` |
| 账号系统 | Supabase 邮箱登录已接入，本地→云端迁移 | `assets/js/auth.js` |
| 结果个性化 | 引用用户具体回答 + 档案 + 其他测试结果 | `assets/js/quiz.js:1000` |

**核心结论**：产品能力已经超过变现能力。最值钱的「AI 深度解读」正在被免费送掉，下一步不是造新功能，而是**给已有能力加门槛、加支付、加数据**。

## 二、目标与原则

1. **免费是钩子，付费买「被理解和被陪伴」**，不是买「更多字」。
2. 付费点放在用户情绪最高的时刻：测完看到结果的那一刻。
3. 所有改动可回退、可埋点、可 A/B。

## 三、结果页三层结构

### L0 免费概览（完成测试即得）

- 结果名称 + 核心特征（1–2 段）
- 「今日一问」 + 1 条行动建议
- 一张可分享的结果卡片（品牌传播）

### L1 分享解锁（个性化内容）

- 内在冲突 + 成长方向 + 专属信件
- 「你的声音」个性化解读
- 触发方式：分享给一位好友并让 TA 打开（V1 验证），或点击分享即解锁（V0 信任制）

### L2 付费深度解读（主变现点）

- AI 深度解读全文（约 3000 字，引用用户具体回答 + 档案）
- 生成后可追问 2–3 轮（对话式）
- 单次定价 9.9 / 19.9 / 29.9 元（按测试深度分档）
- 购买后自动解锁 L1 剩余内容（若尚未分享）

## 四、分享解锁：技术实现

### V0（第一版，本周可上线）

**点击分享即解锁（信任制）**：

- 分享按钮生成带参链接：`https://guanji-lab.vercel.app/test-who.html?from=guan_who&ref=<随机id>`
- 点击分享 → localStorage 写入 `guan_share_<quizKey>=1` → 解锁 L1
- `?from=` 参数仅用于追踪分享来源（朋友打开时上报）

局限：无法真正验证「朋友是否打开」，会被薅。**初期可接受**——目标不是防薅，而是验证分享意愿和转化率。

### V1（第二版，接 Supabase 后）

- 分享链接带 `ref`，朋友打开时调用新接口 `api/claim.js` 登记
- 原用户登录状态下，打开结果页时查询 `share_claims` 表，确认解锁
- 表结构（Supabase）：
  - `share_claims`: id, quiz_key, sharer_id, claimed_by, created_at
  - `entitlements`: user_id, quiz_key, level, expires_at

## 五、付费深度解读：支付接入三步走

### 第一步：面包多（推荐，0 门槛）

- 注册面包多，创建商品「深度解读 · 你与世界相遇的姿态」等
- 商品支持微信/支付宝，无需企业主体、无需备案
- 流程：结果页「解锁深度解读」→ 跳转面包多商品页 → 支付 → 返回携带订单号 → 前端验证并解锁
- 验证方式：面包多支持支付回调（Webhook）或支付完成跳转参数；初期可「支付后返回 + 输入/自动带入订单号」解锁，人工兜底
- 成本：平台抽成约 5–6%

### 第二步：知识星球 / 小报童（会员制托管）

- 适合后续的「三十天陪伴营」「每周来信」
- 网站只做引流，内容与收款在平台内闭环，零开发

### 第三步：微信支付商户号（正规订阅，企业主体后）

- 需要：企业/个体户主体 + 微信认证 + 已备案域名
- 接入：统一下单 → 扫码/JSAPI 支付 → 回调验签 → 更新权益
- 适合：年卡会员、自动续费、规模化运营

**建议节奏**：先走面包多跑通付费验证（1–2 周），数据达标后决定是否升级微信支付。

## 六、埋点与转化漏斗

在 `main.js` 增加 `guanTrack(event, data)` 辅助函数，上报到 Supabase 表 `events`：

| 事件 | 含义 | 目标参考 |
| --- | --- | --- |
| `quiz_start` | 开始测试 | — |
| `quiz_finish` | 完成测试 | — |
| `share_click` | 点击分享 | 完成测试的 30%+ |
| `share_open` | 好友打开分享链接 | — |
| `deep_click` | 点击深度解读 | 看到结果的 20%+ |
| `pay_click` | 点击购买 | 点击解读的 40%+ |
| `pay_success` | 支付成功 | 点击购买的 50%+ |
| `followup_ask` | 追问 | 付费用户的 40%+ |

**核心北极星指标**：测试完成 → 付费 转化率 ≥ 5%。

## 七、改造范围与工作量（单人开发）

| 改动 | 文件 | 工作量 |
| --- | --- | --- |
| 结果页分层渲染 | `assets/js/quiz.js` | 0.5–1 天 |
| 分享解锁（V0） | `assets/js/quiz.js` + `main.js` | 0.5 天 |
| 面包多支付跳转 + 解锁 | `assets/js/quiz.js` + 新增 `pay.html` | 1–2 天 |
| 埋点辅助 | `assets/js/main.js` + Supabase 表 | 0.5 天 |
| 分享登记接口（V1） | 新增 `api/claim.js` | 1 天 |
| 商品创建与测试 | 面包多后台 + 真机走单 | 0.5–1 天 |

总计：**4–7 个工作日**，可完全由你一个人完成。

## 八、转化率预估与风险

### 预估（行业参考区间）

- 测试完成 → 分享解锁：15–30%
- 分享解锁 → 深度解读付费：5–12%
- 测试完成 → 付费（整体）：3–8%
- 单次解读成本：DeepSeek 3000 token 约 0.03–0.1 元，毛利极高

### 风险与对策

| 风险 | 对策 |
| --- | --- |
| 分享信任制被薅 | V0 接受，数据达标后升级 V1 验证 |
| 面包多跳转体验打折 | 支付后自动回跳 + 本地权益即时解锁 |
| 深度解读质量不稳 | 固定 prompt + 温度 0.7；失败自动重试 |
| 用户不登录无法跨设备 | V0 本地解锁；V1 依赖 Supabase 登录 |
| 定价不匹配 | 三种测试分三档，A/B 测 9.9 vs 19.9 |

## 九、上线顺序（建议）

1. **第 1 周**：结果页三层结构 + 分享解锁 V0 + 埋点
2. **第 2 周**：面包多商品 + 支付解锁 + 真机走单
3. **第 3 周**：A/B 测定价 + 看 2 周漏斗数据
4. **数据达标后**：Supabase 权益表 + 分享验证 V1 → 决定是否升级微信支付
