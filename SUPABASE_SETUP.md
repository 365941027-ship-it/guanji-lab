# 关于 Supabase（已停用）

这份文档原本是「如何用 Supabase 接入账号系统」的教程。**现在已经不适用了**，观己实验室的账号系统已迁到自建服务器，因此保留这份说明只为交代来龙去脉。

## 为什么迁走

Supabase 免费档有个硬性限制：**项目连续 7 天没有活动就会被自动暂停**，恢复需要手动操作，长期还会要求升级付费档。对一个「朋友偶尔来用」的小站来说，这意味着随时可能因为没人访问而挂掉，而且暂停后老用户会直接登录不上。既然已经有一台常驻的腾讯云服务器，把账号和档案放在自己手里更稳。

## 现在的账号系统长什么样

| 项目 | 现在怎么做 |
| --- | --- |
| 账号与密码 | 自建 PostgreSQL 的 `users` 表，密码用哈希存储，服务器无法还原原始密码 |
| 登录状态 | 登录成功后发一个 httpOnly Cookie，前端读不到、也偷不走 |
| 用户档案 | 存在 `profiles` 表，登录后跨设备同步 |
| 前端配置 | 不再需要 `assets/js/supabase-config.js`，所有请求走同源 `/api/*` |
| 部署方式 | 见 [deploy/README.md](deploy/README.md) |

## 代码里还剩什么

- `supabase/migrations/*.sql`：早期建表脚本，仅作历史记录，不影响运行。
- `api/claim.js`、`api/keepalive.js`：仍会读 `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` 两个环境变量。
  - `api/keepalive.js` 原本是给 GitHub Actions 定时保活用的，那个定时任务已删除，接口留着但已无人调用。
  - `api/claim.js` 是「分享打开登记」，未配置时返回「未配置」，前端会自动回退到信任制解锁，不影响使用。
  - 这两个环境变量留空即可，见 `deploy/.env.example`。

## 微信扫码登录（当初的设想，仍未实现）

微信扫码需要**微信开放平台网站应用**：企业认证 + 域名 ICP 备案 + 平台审核，个人无法开通。
登录页的 `guanWechatLogin` 仍保留提示入口，等资质就绪后再做。
